# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET         /:org/:repo                         controllers.Application.licenseWithDefaultBranch(org, repo)
GET         /:org/:repo/:version                controllers.Application.license(org, repo, version)
