# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET         /:org/:repo                         controllers.Application.license(org, repo, version = "master")
GET         /:org/:repo/:version                controllers.Application.license(org, repo, version)